name: Empirical CI

on: [push, pull_request]

env:
  BUILD_TYPE: MinSizeRel

jobs:
  build:
    # comment-out this line to run regression tests on your local repo
    #if: ${{ github.repository_owner == "empirical-soft" }}

    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        # https://github.com/actions/virtual-environments
        #os: [macos-10.15, ubuntu-16.04]
        include:
          - name: mac
            os: macos-10.15
            cc: ""
            cxx: ""
          - name: linux
            os: ubuntu-16.04
            cc: gcc-8
            cxx: g++-8

    steps:
      # https://docs.github.com/en/free-pro-team@latest/actions/reference/workflow-syntax-for-github-actions#using-a-specific-shell
      - uses: actions/checkout@v2

      - name: Prep
        if: ${{ matrix.name == 'linux' }}
        run: sudo apt-get install uuid-dev

      - name: Build
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: make prod

      - name: Test
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
        run: make test

      - name: Deploy
        run: make deploy

